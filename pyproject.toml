[project]
name = "lahacks26"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "elevenlabs",
    "fastapi",
    "langchain",
    "langchain-core",
    "langchain-openai",
    "langgraph",
    "openai",
    "pydantic-settings",
    "python-dotenv",
]
